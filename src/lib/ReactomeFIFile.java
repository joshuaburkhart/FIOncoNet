package lib;

/**
 * Created by burkhart on 6/29/16.
 */
public class ReactomeFIFile extends File implements IPairwiseInteractionFile {
    public ReactomeFIFile(String filePath, ILogger logger) {
        super(filePath, logger);
    }
}
